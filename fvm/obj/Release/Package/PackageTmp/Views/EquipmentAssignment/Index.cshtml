@model IEnumerable<fvm.Models.EquipmentAssignment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Contact.ContactDesignator)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Equipment.EquipmentDesignatorVideo2Display)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Contact.ContactDesignator)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Equipment.EquipmentDesignatorVideo2Display)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EquipmentAssignmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EquipmentAssignmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EquipmentAssignmentID })
        </td>
    </tr>
}

</table>
